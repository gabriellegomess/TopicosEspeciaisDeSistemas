export function calcularQuadrado(valor: number): number{
    //oq o valor é(tipo dele), e qual é o tipo de resposta que vai dar
   /* let resultado: number;
    resultado = valor * valor;
    return resultado; */

    return valor * valor;
}

export function calcularSoma(v1: number, v2: number): number{ //(v1: number - variavel e o tipo dela): number - tipo devar q a função vai retornar)

    return v1 + v2;

}